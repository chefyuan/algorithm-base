> 如果阅�?�时，发现错�?，或者动画不�?以显示的�?题可以添加我�?信好�? **[tan45du_one](https://raw.githubusercontent.com/tan45du/tan45du.github.io/master/�?人微�?.15egrcgqd94w.jpg)** ，�?�注 github + 题目 + �?�? 向我反�??
>
> 感谢�?持，该仓库会一直维护，希望对各位有一�?丢帮助�?
>
> 另�?�希望手机阅读的同�?�可以来我的 <u>[**�?众号：�?�厨的算法小�?**](https://raw.githubusercontent.com/tan45du/test/master/�?信图片_20210320152235.2pthdebvh1c0.png)</u> 两个平台同�?�，想�?�和题友一起刷题，互相监督的同学，�?以在我的小屋点击<u>[**刷�?�小�?**](https://raw.githubusercontent.com/tan45du/test/master/�?信图片_20210320152235.2pthdebvh1c0.png)</u>进入�?

#### [136. �?出现一次的数字](https://leetcode-cn.com/problems/single-number/)

> 给定一�?非空整数数组，除了某�?元素�?出现一次以外，其余每个元素均出现两次。找出那�?�?出现了一次的元素�?

示例 1:

> 输入: [2,2,1]
> 输出: 1

示例 2:

> 输入: [4,1,2,1,2]
> 输出: 4

这个题目非常容易理解，就�?让我�?找出那个�?出现一次的数字，那么下面我�?来看一下这几�?�解题方法吧~

### HashMap

#### 解析

�? HashMap 的这�?方法�?很�?�易实现的，题目要求不是让我�?求�?�数嘛，那我�?直接遍历数组将每�?数字和其出现的�?�数存到哈希表里就可以了，然后我�?再从哈希表里找出出现一次的那个数返回即�?�?

![哈希表解法](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/哈希表解�?.1kefww8xsig0.png)

#### 题目代码

Java Code:

```java
class Solution {
    public int singleNumber(int[] nums) {
        //特殊情况
        if (nums.length == 1) {
            return nums[0];
        }
        //HashMap
        HashMap<Integer,Integer> map = new HashMap<Integer,Integer>();
        //将其存入哈希表中，含义为，若该元素不存在则存入表�?，并计数�?1，若已经存在获取次数并加1
        for (int x : nums) {
            map.put(x , map.getOrDefault(x,0) + 1);
        }
        //遍历出出现�?�数�?1的情�?
        for (int y : map.keySet()) {
            if(map.get(y) == 1){
                return y;
            }
        }
    }
}
```

Go Code:

```go
func singleNumber(nums []int) int {
    if len(nums) == 1 {
        return nums[0]
    }
    m := map[int]int{}
    for _, x := range nums {
        m[x]++
    }
    for k, v := range m {
        if v == 1 {
            return k
        }
    }
    return 0
}
```

C++ Code:

```cpp
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        //特殊情况
        if (nums.size() == 1) {
            return nums[0];
        }
        //HashMap
        unordered_map<int,int> map;
        //将其存入哈希表中，含义为，若该元素不存在则存入表�?，并计数�?1，若已经存在获取次数并加1
        for (int x : nums) {
            if (map.find(x) == map.end()) {
                map.insert({x, 0});
            }
            map[x] += 1;
        }
        //遍历出出现�?�数�?1的情�?
        for (int y : nums) {
            if(map.at(y) == 1){
                return y;
            }
        }
        return -1;
    }
};
```

JS Code:

```javascript
var singleNumber = function (nums) {
  //特殊情况
  if (nums.length === 1) {
    return nums[0];
  }
  //HashMap
  map = {};
  //将其存入哈希表中，含义为，若该元素不存在则存入表�?，并计数�?1，若已经存在获取次数并加1
  for (let x of nums) {
    if (!(x in map)) {
      map[x] = 0;
    }
    map[x] += 1;
  }
  //遍历出出现�?�数�?1的情�?
  for (let key in map) {
    if (map[key] === 1) {
      return key;
    }
  }
};
```

Python Code:

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        # 特殊情况
        if len(nums) == 1:
            return nums[0]
        # HashMap
        map_ = {}
        # 将其存入哈希表中，含义为，若该元素不存在则存入表�?，并计数�?1，若已经存在获取次数并加1
        for x in nums:
            map_.setdefault(x, 0)
            map_[x] += 1
        # 遍历出出现�?�数�?1的情�?
        for y, count in map_.items():
            if count == 1:
                return y
```

### 排序搜索�?

#### 解析

这个方法也是特别容易想到的，我们首先对数组进行排序，然后遍历数组，因为数组中其他数字都出现两次，�?有目标值出现一次，所以则让我�?的指针每次跳两�?�，当发现当前值和前一位不一样的情况时，返回前一位即�?，当然我�?需要考虑这�?�情况，当我�?的目标值出现在数组最后一位的情况，所以当数组遍历结束后没有返回值，则我�?需要返回数组最后一位，下面我们看一下动图解析�?

![排序](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/排序.6sp72k3iaqw0.gif)

#### 题目代码

Java Code:

```java
class Solution {
    public int singleNumber(int[] nums) {
        if (nums.length == 1){
            return nums[0];
        }
        //排序
        Arrays.sort(nums);
        for (int i = 1; i < nums.length-1; i+=2){
            if (nums[i] != nums[i-1]){
                return nums[i-1];
            }
        }
        return nums[nums.length-1];

    }
}
```

Go Code:

```go
func singleNumber(nums []int) int {
    if len(nums) == 1 {
        return nums[0]
    }
    sort.Ints(nums)
    for i := 1; i < len(nums) - 1; i+=2 {
        if nums[i] == nums[i - 1] {
            continue
        } else {
            return nums[i - 1]
        }
    }
    return nums[len(nums) - 1]
}
```

C++ Code:

```cpp
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        //排序
        sort(nums.begin(), nums.end());
        for (int i = 1; i < nums.size() - 1; i += 2){
            if (nums[i] != nums[i - 1]){
                return nums[i - 1];
            }
        }
        return nums[nums.size() - 1];
    }
};
```

JS Code:

```javascript
var singleNumber = function (nums) {
  // 排序
  nums.sort();
  for (let i = 1; i < nums.length - 1; i += 2) {
    if (nums[i] != nums[i - 1]) {
      return nums[i - 1];
    }
  }
  return nums[nums.length - 1];
};
```

Python Code:

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        # 排序
        nums.sort()
        for i in range(1, len(nums), 2):
            if nums[i] != nums[i - 1]:
                return nums[i - 1]
        return nums[len(nums) - 1]
```

### HashSet

#### 解析

这个方法也是比较容易实现的，我们利用 HashSet 来完成。HashSet 在我�?刷�?�时出现频率�?特别高的，它�?基于 HashMap 来实现的，是一�?不允许有重�?�元素的集合。那么在这个题解�?，它起到什么作用呢？解题思路如下，我�?依�?�遍历元素并�? HashSet 内的元素进�?�比较，如果 HashSet 内没有�?�元素（说明该元素�??一次出现）则存入，若是 HashSet 已经存在该元�?(�?二�?�出�?)，则将其�? HashSet �?去除，并继续遍历下一�?元素。最�? HashSet 内剩下的则为我们的目标数。思路和我�?之前说过的括号匹配问题类似，我们一起来看一下动图解析吧�?

![HashSet](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/HashSet.4b6dcxwj07c0.gif)

#### 题目代码

Java Code:

```java
class Solution {
    public int singleNumber(int[] nums) {
         HashSet<Integer> set = new HashSet<>();
         //�?�?遍历
         for (int x : nums){
             //已经存在，则去除
             if(set.contains(x)){
                 set.remove(x);
             //否则存入
             } else {
                 set.add(x);
             }
         }
         //返回仅剩的一�?元素
         return set.iterator().next();
    }
}
```

C++ Code:

```cpp
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        unordered_set<int> set;
        //�?�?遍历
        for (int x : nums) {
            //已经存在，则去除
            if (set.find(x) == set.end()) {
                set.insert(x);
            //否则存入
            } else {
                set.erase(x);
            }
        }
        //返回仅剩的一�?元素
        return *set.begin();
    }
};
```

JS Code:

```javascript
var singleNumber = function (nums) {
  let set = new Set();
  //�?�?遍历
  for (let x of nums) {
    //已经存在，则去除
    if (set.has(x)) {
      set.delete(x);
      //否则存入
    } else {
      set.add(x);
    }
  }
  return set.values().next().value;
};
```

Python Code:

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        set_ = set()
        # �?�?遍历
        for x in nums:
            # 已经存在，则去除
            if x in set_:
                set_.remove(x)
            # 否则存入
            else:
                set_.add(x)
        # 返回仅剩的一�?元素
        return set_.pop()
```

### �?

#### 解析

该方法也很�?�易想到，我�?首先将其排序，然后遍历数组，如果栈为空则将当前元素压入栈，�?�果栈不为空，若当前元素和栈顶元素相同则出栈，继�?遍历下一元素，�?�果当前元素和栈顶元素不同的话，则�?�明栈顶元素�?�?出现一次的元素，我�?将其返回即可。这�?题目也可以使用队列做，思路一致，我们就不在这里�?�明啦。下面我�?看下动图解析�?

![栈](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/�?.6mzstgebww00.gif)

#### 题目代码

Java Code:

```java
class Solution {
    public int singleNumber(int[] nums) {
        Arrays.sort(nums);
        Stack<Integer> stack = new Stack<>();
        for (int x : nums){
           if (stack.isEmpty()) {
               stack.push(x);
               continue;
           }
           //不同时直接跳�?
           if (stack.peek() != x) {
               break;
           }
           //相同时出�?
           stack.pop();
        }
        return stack.peek();
    }
}
```

C++ Code:

```cpp
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        stack<int> stack;
        for (int x: nums) {
            if (stack.empty()) {
                stack.push(x);
                continue;
            }
            //不同时直接跳�?
            if (stack.top() != x) {
                break;
            }
            //相同时出�?
            stack.pop();
        }
        return stack.top();
    }
};
```

JS Code:

```javascript
var singleNumber = function (nums) {
  nums.sort();
  let stack = [];
  for (let x of nums) {
    if (!stack.length) {
      stack.push(x);
      continue;
    }
    //不同时直接跳�?
    if (stack[0] !== x) {
      break;
    }
    //相同时出�?
    stack.pop();
  }
  return stack[0];
};
```

Python Code:

```python
from collections import deque

class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
        nums.sort()
        stack = deque()
        for x in nums:
            if not stack:
                stack.append(x)
                continue
            # 不同时直接跳�?
            if stack[-1] != x:
                break
            # 相同时出�?
            stack.pop()
        return stack[-1]
```

### 求和�?

#### 解析

这个方法也比较简单，也是借助咱们�? HashSet ，具体思路如下，我�?通过 HashSet 保存数组内的元素，然后进行求和（setsum），那么得到的这�?和则为去除掉重�?�元素的和，我们也可以得到所有元素和（numsum）。因为我�?其他元素都出现两次，仅有一�?元素出现一次，那我�?通过 setsum \* 2 - numsum 得到的元素则为出现一次的数�?

![求和解法](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/求和解法.2tds49a3vzq0.png)

上面我们�? SetSum \* 2 - NumSum = z 也就�?我们所求的值， �?不是感�?�很简单呀�?

#### 题目代码

Java Code:

```java
class Solution {
    public int singleNumber(int[] nums) {
       HashSet<Integer> set = new HashSet<>();
       int setsum = 0;
       int numsum = 0;
       for (int x : nums) {
           //所有元素的�?
           numsum += x;
           if (!set.contains(x)) {
               //HashSet内元素的�?
               setsum += x;
           }
           set.add(x);
       }
       //返回�?
       return setsum * 2 - numsum;
    }
}
```

C++ Code:

```cpp
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        unordered_set<int> set;
        int setsum = 0;
        int numsum = 0;
        for (int x : nums) {
           //所有元素的�?
           numsum += x;
           if (set.find(x) == set.end()) {
               //HashSet内元素的�?
               setsum += x;
           }
           set.insert(x);
        }
        //返回�?
        return setsum * 2 - numsum;
    }
};
```

JS Code:

```javascript
var singleNumber = function (nums) {
  let set = new Set();
  let setsum = 0;
  let numsum = 0;
  for (let x of nums) {
    //所有元素的�?
    numsum += x;
    if (!set.has(x)) {
      setsum += x;
    }
    //HashSet内元素的�?
    set.add(x);
  }
  //返回�?
  return 2 * setsum - numsum;
};
```

Python Code:

```python
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        return 2 * sum(set(nums)) - sum(nums)
```

### 位运�?

#### 解析

这个方法主�?�是借助咱们的位运算�? ^ 按位异或，我�?先来了解一下这�?位运算�?��?

> 按位异或（XOR）运算�?�“^”是双目运算符�? 其功能是参与运算的两数各对应的二进位相异或，当两对应的二进位相异时，结果�? 1。相同时�? 0�?

> 任何数和 0 异或，仍为本�?：a�?0 = a
> 任何数和�?�?异或，为 0：a⊕a = 0
> 异或运算满足交换律和结合律：a⊕b⊕a = (a⊕a)⊕b = 0⊕b = b

例：

![异或运算](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/异或运算.1myeo11xgqo0.png)

我们通过上面的例子了解了异或运算，�?�应位相异时�? 1，相同时�? 0，那么某�?数跟�?�?异或时，因为对应位都相同所以结果为 0 �? 然后异或又满足交换律和结合律。则

![image-20201129120648802](https://cdn.jsdelivr.net/gh/tan45du/tan45du.github.io.photo@master/photo/image-20201129120648802.2ajgng6zzd7o.png)

#### 题目代码

Java Code:

```java
class Solution {
    public int singleNumber(int[] nums) {
        int num = 0;
        //异或
        for (int x : nums) {
              num ^= x;
        }
        return num;
    }
}
```

Go Code:

```go
func singleNumber(nums []int) int {
    res := 0
    for _, x := range nums {
        res ^= x
    }
    return res
    
}
```

�?题一共介绍了6种解题方法，�?定还有别的方法，欢迎大�?��?��?�。大家可以在做�?�的时候一题�?�解。这样能大大提高�?己解题能力。下面我�?来看一下这些方法�?�何应用到其他�?�目上�?
C++ Code:

```cpp
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int num = 0;
        //异或
        for (vector<int>::iterator x = nums.begin(); x != nums.end(); x++) {
            num ^= *x;
        }
        return num;
    }
};
```

JS Code:

```javascript
var singleNumber = function (nums) {
  //异或
  return nums.reduce((num, x) => (num ^= x));
};
```

Python Code:

```python
from functools import reduce

class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        # 异或
        return reduce(lambda num, x: num ^ x, nums, 0)
```

�?题一共介绍了 6 种解题方法，�?定还有别的方法，欢迎大�?��?��?�。大家可以在做�?�的时候一题�?�解。这样能大大提高�?己解题能力。下面我�?来看一下这些方法�?�何应用到其他�?�目上�?
